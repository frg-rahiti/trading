import Link from 'next/link';import {createServerSupabase} from '../../lib/supabase-server';
export default async function Dashboard(){const supabase=await createServerSupabase();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main className="container section"><h1>Dashboard</h1><p className="muted">You need to sign in to access your account.</p><Link className="btn btn-primary" href="/login">Sign in</Link></main>;const {data:licenses}=await supabase.from('licenses').select('id,license_key,license_type,status,products(name,platform)').eq('user_id',user.id);return <main className="container section"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><span className="pill">CUSTOMER DASHBOARD</span><h1 style={{fontSize:48,letterSpacing:'-.04em'}}>Your workspace</h1><p className="muted">{user.email}</p></div><Link href="/products">Browse products →</Link></div><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(210px,1fr))',marginTop:35}}>{[['Products',licenses?.length??0],['Active licenses',licenses?.filter((x:any)=>x.status==='ACTIVE').length??0],['Downloads','—']].map(([a,b])=><div className="card stat" key={a as string}><span className="muted">{a}</span><br/><strong>{b}</strong></div>)}</div><section style={{marginTop:40}}><h2>Your licenses</h2>{licenses?.length?<div className="grid" style={{marginTop:18}}>{licenses.map((l:any)=><article className="card" style={{padding:24}} key={l.id}><div style={{display:'flex',justifyContent:'space-between'}}><b>{l.products?.name}</b><span className="pill">{l.status}</span></div><p className="muted">{l.products?.platform} · {l.license_type}</p><code>{l.license_key}</code><div style={{marginTop:18}}><Link href="/dashboard/downloads">Manage downloads →</Link></div></article>)}</div>:<div className="card" style={{padding:30}}><p className="muted">No licenses yet. Purchased products will appear here.</p><Link href="/products">Explore products →</Link></div>}</section></main>}
