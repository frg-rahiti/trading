'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {createClient} from '../../lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();setMessage('Signing in…');const {error}=await createClient().auth.signInWithPassword({email,password});if(error){setMessage(error.message);return}window.location.href='/dashboard'}return <main className="container" style={{minHeight:'100vh',display:'grid',placeItems:'center'}}><div className="card" style={{width:'min(440px,100%)',padding:34}}><Link className="brand" href="/">TAHITIAN <span className="gradient">TRADER</span></Link><h1 style={{fontSize:36,marginBottom:8}}>Welcome back</h1><p className="muted">Access your products, licenses and downloads.</p><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:25}}><input aria-label="Email" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required style={{padding:14,border:'1px solid var(--line)',borderRadius:10}}/><input aria-label="Password" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required style={{padding:14,border:'1px solid var(--line)',borderRadius:10}}/><button className="btn btn-primary" style={{border:0,cursor:'pointer'}}>Sign in</button></form>{message&&<p className="muted" style={{marginTop:15}}>{message}</p>}<p className="muted" style={{marginTop:22,fontSize:14}}>New here? <Link href="/signup">Create an account</Link></p></div></main>}
