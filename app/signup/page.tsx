'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {createClient} from '../../lib/supabase';
export default function Signup(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();setMessage('Creating account…');const {error}=await createClient().auth.signUp({email,password});setMessage(error?error.message:'Account created. Check your email to confirm your address.')}return <main className="container" style={{minHeight:'100vh',display:'grid',placeItems:'center'}}><div className="card" style={{width:'min(440px,100%)',padding:34}}><Link className="brand" href="/">TAHITIAN <span className="gradient">TRADER</span></Link><h1 style={{fontSize:36,marginBottom:8}}>Create your account</h1><p className="muted">Your purchased products and licenses will live here.</p><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:25}}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required style={{padding:14,border:'1px solid var(--line)',borderRadius:10}}/><input type="password" minLength={8} placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} required style={{padding:14,border:'1px solid var(--line)',borderRadius:10}}/><button className="btn btn-primary" style={{border:0,cursor:'pointer'}}>Create account</button></form>{message&&<p className="muted" style={{marginTop:15}}>{message}</p>}<p className="muted" style={{marginTop:22,fontSize:14}}>Already have an account? <Link href="/login">Sign in</Link></p></div></main>}
